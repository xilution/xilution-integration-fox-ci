version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
  build:
    commands:
      - |
        ./scripts/test.sh \
          $FOX_PIPELINE_ID \
          $STAGE_NAME \
          $CLIENT_AWS_REGION \
          $CODEBUILD_SRC_DIR_SourceCode
