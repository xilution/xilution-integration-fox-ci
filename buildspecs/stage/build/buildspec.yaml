version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
  build:
    commands:
      - |
        ./scripts/build.sh \
          $STAGE_NAME \
          $CODEBUILD_SRC_DIR_SourceCode
artifacts:
  files:
    - dist.zip
