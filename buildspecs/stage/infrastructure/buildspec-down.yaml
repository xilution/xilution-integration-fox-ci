version: 0.2

phases:
  pre_build:
    commands:
      - . ./scripts/assume-role.sh
      - ./scripts/terraform-init-stage.sh
  build:
    commands:
      - ./scripts/additional-infrastructure-down-stage.sh
      - ./scripts/terraform-destroy-stage.sh
      - ./scripts/cloudformation-delete-stack-stage.sh
