version: 0.2

phases:
  pre_build:
    commands:
      - . ./scripts/assume-role.sh
  build:
    commands:
      - ./scripts/additional-infrastructure-stage.sh down
      - ./scripts/terraform-stage.sh down
